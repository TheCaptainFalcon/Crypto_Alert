import React, { Component } from 'react';

class CustomAlert extends Component {
    constructor(props) {
        super(props);
        this.state = {  }
    }
    render() { 
        return (  
            <div>
                this is the component for specifying token addresses with types.
            </div>
        );
    }
}
 
export default CustomAlert;